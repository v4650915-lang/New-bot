# Инструкция по деплою через GitHub и Timeweb

У вас уже есть всё необходимое! Использование GitHub значительно упростит процесс обновлений.

## 1. Загрузка кода на GitHub (на вашем ПК)
В папке с проектом уже выполнены начальные настройки Git. Вам нужно:

1. Создать **новый пустой репозиторий** на сайте GitHub.
2. Выполнить следующие команды в терминале (в папке бота), заменив URL на ваш:

```powershell
git add .
git commit -m "Initial deploy"
git branch -M main
git remote add origin https://github.com/ВАШ_НИК/ВАШ_РЕПОЗИТОРИЙ.git
git push -u origin main
```
*Примечание: файл `.env` с паролями и токенами специально исключен из Git (в `.gitignore`), чтобы его не украли. На сервере мы создадим его вручную.*

## 2. Настройка сервера (Timeweb)
Зайдите на сервер через терминал (консоль Timeweb или PuTTY):
`ssh root@IP_ВАШЕГО_СЕРВЕРА`

### Установка ПО (один раз)
```bash
apt update
apt install python3-pip python3-venv git -y
```

### Скачивание бота
Перейдите в папку, где будет жить бот (например, `/opt`):
```bash
cd /opt
git clone https://github.com/ВАШ_НИК/ВАШ_РЕПОЗИТОРИЙ.git bot
cd bot
```

### Настройка окружения
1. Создайте виртуальное окружение:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```
2. **Важно!** Создайте файл настроек `.env`:
   ```bash
   nano .env
   ```
   Вставьте туда содержимое вашего локального файла `.env` (токены, ID админа и т.д.).
   Нажмите `Ctrl+O` (Enter) для сохранения и `Ctrl+X` для выхода.

## 3. Запуск в фоновом режиме (Systemd)
Чтобы бот работал вечно и сам перезапускался:

1. Создайте файл службы:
   ```bash
   nano /etc/systemd/system/bot.service
   ```
2. Вставьте этот текст:
   ```ini
   [Unit]
   Description=Telegram Bot
   After=network.target

   [Service]
   User=root
   WorkingDirectory=/opt/bot
   ExecStart=/opt/bot/venv/bin/python main.py
   Restart=always

   [Install]
   WantedBy=multi-user.target
   ```
3. Запустите:
   ```bash
   systemctl enable bot
   systemctl start bot
   ```

## 🌐 Как вносить обновления?
Когда нужно что-то поменять:
1. **На ПК**: меняете код -> `git add .` -> `git commit -m "fix"` -> `git push`
2. **На сервере**:
   ```bash
   cd /opt/bot
   git pull
   systemctl restart bot
   ```
Всё! Бот обновился.
